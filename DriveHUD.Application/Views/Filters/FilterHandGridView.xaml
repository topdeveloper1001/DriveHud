<UserControl x:Class="DriveHUD.Application.Views.FilterHandGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:DriveHUD.Application.Views"
             xmlns:vp="clr-namespace:DriveHUD.Application.Views.PopupContainers"
             xmlns:vm="clr-namespace:DriveHUD.Application.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <ContentPresenter Content="{Binding SelectedViewModel}" />
        </StackPanel>
    </Grid>
</UserControl>
