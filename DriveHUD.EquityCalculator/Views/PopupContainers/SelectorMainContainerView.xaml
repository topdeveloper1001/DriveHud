<UserControl x:Class="DriveHUD.EquityCalculator.Views.SelectorMainContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DriveHUD.EquityCalculator.Views"
             xmlns:vm="clr-namespace:DriveHUD.EquityCalculator.ViewModels"
             xmlns:intr="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:prism="http://www.codeplex.com/prism"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <intr:Interaction.Triggers>
        <intr:EventTrigger EventName="Loaded">
            <intr:InvokeCommandAction Command="{Binding OnViewLoadedCommand}"/>
        </intr:EventTrigger>
    </intr:Interaction.Triggers>
    
    <UserControl.DataContext>
        <vm:SelectorMainContainerViewModel />
    </UserControl.DataContext>
    <Grid>
        <ContentControl Content="{Binding SelectedView}"></ContentControl>
    </Grid>
</UserControl>
